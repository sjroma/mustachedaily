<!DOCTYPE html>
<html>

<head>
  <link rel="stylesheet" href="main.css">
</head>

<body>
  <div class="container">
<!--    {{ #users }}-->
    <div class="bots">
      <div div id="bot_container">
        <img src = "{{avatar}}" alt="cartoon robot picture"> 
        <div id="name">{{ name }}</div>
        <div id="job">{{ job }}</div>
        <div id="company">{{ company }}</div>
      </div>
<!--    {{ /users }}-->
    </div>
  </div>
</body>

</html>
